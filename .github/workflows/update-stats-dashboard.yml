name: Update Stats Dashboard
on:
  push:
    branches: [master]
  workflow_dispatch:
permissions:
  contents: write
  issues: read
  pull_requests: read
jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
